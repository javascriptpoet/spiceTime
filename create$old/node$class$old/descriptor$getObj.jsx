export default ()=>({scope,
    wrappedLocal:node
})=>({
    me:childSpaceTime,
    $:{
        me:child$,
        name:childName,
        p$:{local$:pLocal$={}}
    }
})=>{
    pLocal$[childName]=child$
    child$.locals$getObj
    const descriptor=pDescriptor$getObj()
}